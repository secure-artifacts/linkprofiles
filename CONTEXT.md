# Link Profile

个人链接聚合页。社媒平台（TikTok、Instagram）的简介栏只允许放一条链接，导致本应流向 WhatsApp、Messenger 的线索流失。本项目提供一个可自定义的公开页面，把多个渠道收拢到一条链接背后，并统计哪些渠道真正带来了线索。

## 领域语言

### 页面与身份

**个人页 (Profile Page)**:
`域名/{short_name}` 下渲染的公开页面，包含头像、简介、社媒图标与按钮列表。每个用户恰好拥有一个。仅「用户」角色拥有个人页，超级管理员与管理员是纯后台运营者。
_避免使用_：短链接、落地页、主页

四个标识字段互不兼任，任何时候都不要混用：

**账号 (Account)**:
登录凭证中的标识，全站唯一。仅用于登录，不出现在任何对外页面上。
_避免使用_：用户名、username、登录名

**用户名称 (User Label)**:
中文备注，用途是让管理员在后台辨认这个账号属于谁。可重复，不做唯一约束，不出现在个人页上。
_避免使用_：昵称、显示名、姓名

**short_name**:
个人页在 URL 中的唯一标识，全站唯一，且不得与保留路径冲突。限 URL 安全字符。一经发布即为对外资产，修改会使已发出的链接失效。
_避免使用_：username、slug、handle

**显示名 (Display Name)**:
个人页头像下方展示给访客的名字。可中文、可含 emoji、可重复，随时修改且不影响个人页地址。
_避免使用_：昵称、用户名、short_name

**归属管理员 (Owning Admin)**:
创建了某个用户的管理员。管理员只能看见并管理归属于自己的用户，超级管理员不受此限。归属管理员被删除后，其名下用户转为**无归属**，仅超级管理员可见，可再指派。
_避免使用_：负责人、所属、owner

### 页面元素

**布局 (Layout)**:
个人页的整体排版形态，取值为 Classic、Hero、Banner、Cutout、Shape 五种之一。决定头像/头图区域的形状与占比，不决定配色。
_避免使用_：模板、主题、样式

**按钮 (Button)**:
个人页主体区域中一个可点击的条目，带自定义文字和一行选填的副标题，指向一个可随时更换的目标链接。全部按钮共处一个用户可自由排序的列表，不分组、无区段标题；联系类与内容类的视觉差异只由 `is_lead` 决定。
_避免使用_：链接项、卡片、link

**副标题 (Button Subtitle)**:
按钮标题下方的一行选填说明，用途是降低访客开口的顾虑（如「通常当天回复」）。留空则不渲染。
_避免使用_：描述、备注、简介

**社媒图标 (Social Icon)**:
个人页头部的一组图标式入口，从系统内置清单中逐个启用。清单**仅含海外平台，绝对不含任何大陆 app**。Messenger 是独立图标，与 Facebook 分开——前者指向 `m.me` 直接开对话，后者只是主页。与按钮的区别在于展现形式和位置，不在于行为。
_避免使用_：社交链接

### 数据分析

**线索 (Lead)**:
访客对**联系类渠道**的一次点击。联系类由每个按钮和社媒图标上的 `is_lead` 标记决定：WhatsApp、Messenger、email 默认为是，Instagram、YouTube 默认为否，可在后台逐条修改。点击非联系类渠道产生点击，但不产生线索。
_避免使用_：转化、询盘、客户

**点击 (Click)**:
访客对任意按钮或社媒图标的一次点击，不区分是否联系类。线索是点击的子集。
_避免使用_：访问、tap

**页面浏览 (Page View)**:
个人页被渲染一次。作为点击率的分母。
_避免使用_：访问量、UV

**来源 (Source)**:
访客到达个人页时携带的渠道标识，取自地址上的 `src` 查询参数（如 `?src=tiktok`）。同一个个人页可以带不同 `src` 投放到不同平台，用于回答「哪个平台带来的线索最多」。无参数时记为未知来源。
_避免使用_：渠道、utm、referrer

**点击率 (CTR)**:
单个按钮的点击数除以该页面的页面浏览数。页面级汇总指标为全部点击数除以页面浏览数。分母永远是页面浏览，不是访客数。

## 术语禁令

**短链接**：需求文档标题使用了这个词，但本项目不提供长 URL 压缩服务。代码、注释、文档、commit 中一律不得出现，避免与 `域名/{short_name}` 的个人页混淆。
