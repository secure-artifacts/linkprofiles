# Link Profile

个人链接聚合页。社媒平台（TikTok、Instagram）的简介栏只允许放一条链接，导致本应流向 WhatsApp、Messenger 的线索流失。本项目提供一个可自定义的公开页面，把多个渠道收拢到一条链接背后，并统计哪些渠道真正带来了线索。

## 领域语言

### 页面与身份

**个人页 (Profile Page)**:
`域名/{short_name}` 下渲染的公开页面，包含头像、简介与一列条目。**一个用户可以拥有多个**，数量不设上限，各自有独立的地址、布局、素材与埋点数据。仅「用户」角色拥有个人页，超级管理员与管理员是纯后台运营者，名下没有个人页。
_避免使用_：短链接、落地页、主页

**账号与个人页是两样东西**：账号是登录主体，个人页是对外资产。删一个个人页只退休它自己的地址，删账号则名下全部个人页的地址一并退休。

用户自己**建得了**新的个人页、**改得了**自己页面的地址；**删不了** —— 删是唯一不可逆的那个（地址进墓碑永不再分配、媒体从磁盘删除），留给管理员。

四个标识字段互不兼任，任何时候都不要混用：

**账号 (Account)**:
登录凭证中的标识，全站按小写唯一。仅用于登录，不出现在任何对外页面上。用户可验证当前密码后自助修改；管理员可修改名下用户，超级管理员可修改任意用户与管理员。修改后该账号的全部会话失效，并留一条账号变更流水。
_避免使用_：用户名、username、登录名

新建或修改账号时统一为 3–32 位小写字母、数字、点、下划线或横线，首尾必须是字母或数字，特殊符号不得连续。历史宽松账号不强制重写，仍可继续登录。

**账号变更流水 (Account Name Change)**:
一次登录账号改名的记录：改前、改后、谁改的、什么时候。它只用于审计，不给旧账号建立登录别名；改名后旧账号立即失效。

**用户名称 (User Label)**:
中文备注，用途是让管理员在后台辨认这个账号属于谁。可重复，不做唯一约束，不出现在个人页上。
_避免使用_：昵称、显示名、姓名

**short_name**:
个人页在 URL 中的唯一标识，全站唯一，且不得与保留路径冲突。限 URL 安全字符。一经发布即为对外资产，修改会使已发出的链接失效。**属于个人页而不属于账号** —— 一个账号有几个个人页就有几个 short_name。用户自己改得了，改前要二次确认，每次改动都留一条**地址变更流水**。
_避免使用_：username、slug、handle

**显示名 (Display Name)**:
个人页头像下方展示给访客的名字。可中文、可含 emoji、可重复，随时修改且不影响个人页地址。同 short_name，属于个人页而不属于账号。
_避免使用_：昵称、用户名、short_name

**地址变更流水 (Short Name Change)**:
一次改地址的记录：改前、改后、谁改的、什么时候。用途是回退——照着流水把地址改回去。与**墓碑**是两回事：墓碑记的是删除产生的、永不再分配的地址；流水记的是改名，旧地址会被释放，能改回来，也可能被别人抢注。
_避免使用_：日志、历史记录、审计

**归属管理员 (Owning Admin)**:
创建了某个用户的管理员。管理员只能看见并管理归属于自己的用户，超级管理员不受此限。归属管理员被删除后，其名下用户转为**无归属**，仅超级管理员可见，可再指派。
_避免使用_：负责人、所属、owner

### 页面元素

**布局 (Layout)**:
个人页的整体排版形态，取值为 Classic、Hero、Banner、Shape 四种之一。决定头像/头图区域的形状与占比，不决定配色。
_避免使用_：模板、主题、样式

**头像位 (Avatar Slot)**:
个人页代表人物或品牌的方形图片或短视频。它只服务头像与 Hero 头图，不兼任 Banner 图或背景图。
_避免使用_：头图、Banner 图

**Banner 图 (Banner Image)**:
仅供 Banner 布局顶部横幅使用的宽幅图片，与头像位和背景图彼此独立。没有 Banner 图时以当前主题填充，不拿头像位兜底。
_避免使用_：头像、背景图、头图

**背景图 (Background Image)**:
铺在整张个人页背后的图片，与头像位和 Banner 图彼此独立；它覆盖主题背景渐变，但不改变主题的文字与条目配色。
_避免使用_：Banner 图、头像

**条目 (Entry)**:
个人页主体区域中一个可点击的行。两种来源：**链接**由用户自己填目标地址；**社媒入口**从内置清单启用，用户填号码 / 邮箱 / 用户名，地址由系统拼。两者渲染成同一种卡片，共处一个可自由排序的列表，不分组、无区段标题。
_避免使用_：按钮项、卡片、item

**实心背景 (Solid Background)**:
一个页面上的条目一律是实心卡片，还是一律是描边行。**整页统一，不逐条配**，也**与「算不算线索」无关** —— 计入线索的条目并不会因此长得不一样。想让某一条更显眼，靠把它排在前面，见 ADR-0013。
_避免使用_：高亮、主按钮、一级

**图标白底 (Icon Plate)**:
条目左侧品牌图形背后那枚白色圆角衬底。同样整页统一，可以关掉。与「实心背景」是两个独立开关：描边行也可以带白底，实心卡片也可以不带。
_避免使用_：图标背景、icon badge

**描述 (Entry Subtitle)**:
条目标题下方的一行选填说明，用途是降低访客开口的顾虑（如「通常当天回复」）。留空则不渲染。实心卡片与描边行都可以有。
_避免使用_：备注、简介

**社媒入口 (Social Entry)**:
预置了品牌图标与地址拼装规则的条目，从系统内置清单中逐个启用，同一平台只能启用一次。清单**仅含海外平台，绝对不含任何大陆 app**。Messenger 是独立入口，与 Facebook 分开——前者指向 `m.me` 直接开对话，后者只是主页。

它与链接的区别**只在地址从哪来**：用户填的是号码 / 邮箱 / 用户名，不是拼好的 URL。展现形式与排序位置和链接完全一致，见 ADR-0011。
_避免使用_：社媒图标、社交链接

### 数据分析

**线索 (Lead)**:
访客对**联系类渠道**的一次点击。联系类由每个条目上的 `is_lead` 标记决定：WhatsApp、Messenger、email 默认为是，Instagram、YouTube 默认为否，可在后台逐条修改。点击非联系类渠道产生点击，但不产生线索。
_避免使用_：转化、询盘、客户

**点击 (Click)**:
访客对任意条目的一次点击，不区分是否联系类。线索是点击的子集。
_避免使用_：访问、tap

**页面浏览 (Page View)**:
个人页被渲染一次。作为点击率的分母。
_避免使用_：访问量、UV

**来源 (Source)**:
访客到达个人页时携带的渠道标识，取自地址上的 `src` 查询参数（如 `?src=tiktok`）。同一个个人页可以带不同 `src` 投放到不同平台，用于回答「哪个平台带来的线索最多」。无参数时记为未知来源。
_避免使用_：渠道、utm、referrer

**点击率 (CTR)**:
单个条目的点击数除以该页面的页面浏览数。页面级汇总指标为全部点击数除以页面浏览数。分母永远是页面浏览，不是访客数。

**分析层级 (Analytics Scope)**:
管理员的无筛选入口只比较可见账号；账号汇总只比较该账号名下的个人页；单个个人页才展示来源、国家、设备、时段与条目细分。账号指标必须等于名下个人页指标之和，见 ADR-0015。

**转化概览 (Conversion Overview)**:
单个个人页内，来源排行、国家热力图和联系方式排行是交叉明细的图形摘要。它们与明细共用 `crossBreakdowns`，不得单独查询或产生另一套数字。

## 术语禁令

**短链接**：需求文档标题使用了这个词，但本项目不提供长 URL 压缩服务。代码、注释、文档、commit 中一律不得出现，避免与 `域名/{short_name}` 的个人页混淆。
