/**
 * 简介逐字打出的每字间隔。
 *
 * 公开页的内联脚本与后台预览的 useEffect 是刻意重复的两份实现
 * （前者是零构建步骤的裸字符串，import 不了模块，见 ADR-0012），
 * 但这个数字必须是一份：两边打字速度不一致，预览就不再是预览。
 * 内联脚本那边靠模板字符串插值拿到它。
 *
 * 简介上限 300 字，按这个速度写满要打二十多秒 —— 觉得太久就关掉开关。
 */
export const TYPEWRITER_STEP_MS = 70;
