# 01：区域表与数据迁移

**构建内容：** 库里出现区域这一层，现有每个用户都落进了正确的区域，但还没有任何代码去读它。升级完之后系统的行为与升级前完全一样——这张票的价值全在于让下一张票可以安全地切换归属。

按 expand–contract 的第一步做：新形态与旧形态并存，归属管理员字段先留着不删。

**阻塞项：** 多语言支持（`.scratch/i18n/`）合并完成。

**状态：** ready-for-agent

- [x] 区域表落地：名称全站唯一、归属管理员可空（管理员被删时置空）、标记是否默认区域
- [x] 账号表新增区域外键，列上可空，区域被删除时限制而非级联
- [x] 迁移插入唯一的一个「未分配」区域，主键固定，归属为空
- [x] 「未分配」区域由迁移 SQL 创建，不放在超级管理员初始化流程里——那段代码测试上下文不经过
- [x] 迁移为每个管理员建一个默认区域，名称取其用户名称，为空则取账号，与已有名称冲突时追加序号
- [x] 归属于某个管理员的用户写进该管理员的默认区域
- [x] 原本无归属的用户，以及归属指向超级管理员的用户，都写进「未分配」区域
- [x] 归属管理员列本张票不删，检查约束本张票不加
- [x] 测试工厂扩展：能指定区域、能造区域，不传区域时落「未分配」，既有调用点不必逐个改
- [x] 迁移测试覆盖上述每条搬迁规则，并断言迁移前后逐人可见性一致
- [x] 现有全部测试原样通过，一个都不用改
