# 更新记录

## v1.1.2 — 2026-09-10 — 注册页与登录页易用性

登录页与注册页可以直接切界面语言，注册时的页面地址改成边填边看完整网址，并修正几处会误导人的提示。

### 注册与登录

- 登录页与注册页右上角加了语言切换，看不懂当前语言时不必先登录进去才能改。选过的语言这台设备会记住。
- 注册时的「页面地址」左侧固定显示站点域名，只需填后面那一段。
- 把整条网址粘进去会自动只留下地址那一段，不再因为多了域名而报格式错。
- 边填边显示这个地址完整长什么样，以及能不能用。

### 修复

- 注册时地址格式不对（太短、含大写或下划线）会被提示成「已被占用」，现在直接说清是哪条规则没过。
- 注册时填到已删除个人页留下的地址，此前也提示「已被占用」，让人以为换个人就能用；现在说明这个地址永不再分配。
- 面向使用者的提示不再出现 short_name 这个字段名，统一叫「页面地址」。

### 升级提示

- 从 v1.1.1 升级没有新增数据库迁移。

## v1.1.1 — 2026-09-09 — 部署与运维交接

部署失败后可以重新部署同一版本，修复回滚被跳过的问题，并补齐首次部署与故障处理说明。

### 部署与维护

- 部署过程中失败即停止，健康检查通过后才记录成功版本。
- 同一版本也可重新部署，升级失败后恢复旧版不再被跳过。
- 运维手册补齐服务器权限、域名证书、配置编辑和部署验收步骤。
- 补充视频头像上传、自助注册出网、访客 IP 脱敏和地域库配置说明。

### 升级提示

- 从 v1.1.0 升级没有新增数据库迁移。
- 部署和重复部署都会短暂停机，请安排维护时间。

## v1.1.0 — 2026-09-09 — 多语言、区域归属与自助注册

后台与公开页支持七种语言，归属改由区域推导，并加上凭邀请码的自助注册。数据库变更包含迁移 `0018` 至 `0024`，升级前请先读升级提示。

### 多语言

- 后台与公开页支持英语、简体中文、菲律宾语、西班牙语、葡萄牙语（巴西）、印尼语、越南语。
- 界面语言属于账号，页面语言属于个人页，两者互不影响：运营用菲律宾语看后台，个人页照样可以是葡萄牙语。
- 语言切换器放在顶栏，带国旗，随时可切，不必进设置页。
- 日期、数字与百分比按当前语言格式化；报错文案由服务端按请求语言下发。

### 区域归属与自助注册

- 新增「区域」：用户归属由「用户 → 区域 → 管理员」推导，不再挂在用户身上。每个管理员有一个默认区域，新建的用户落进去。
- 每个区域一个邀请码，可复制、可重置，也可以自己指定。
- 注册页支持凭邀请码自助开号，注册出来的账号自动落进邀请码对应的区域。自助注册默认关闭，由超级管理员在全站设置里打开。
- 注册页加入 reCAPTCHA v2 人机验证；用了 Google 公开测试密钥时后台会明确警告。
- 区域支持批量创建，一行一个名字，重名与超长的行单独报出来，不影响其余行。
- 默认区域与还有用户的区域删不掉。删除按钮照常显示但禁用，鼠标移上去说明原因。

### 用户与区域管理

- 用户列表改为「区域 → 用户」两层展开，父行可折叠，可整组勾选后一次移区。
- 用户列表加搜索，账号、后台备注、页面地址与显示名都搜得到，管理员只搜得到自己名下的人。
- 批量创建用户的结果按原文的真实行号回放，中间有空行也不会错位，成功的行直接显示建出来的页面地址。

### 数据分析

- 时间范围、区域与时区的筛选条改为底部悬浮，翻到页面下方也能随手切换。
- 区域与时区下拉支持搜索。
- 新增导出 CSV：把当前筛选范围内的合计、趋势、区域、账号、个人页、来源、国家、设备与联系方式一次导出，可直接导入表格。

### 修复

- 分析页从某个区域切回「全部区域」时白屏。
- 后台多处报错只显示原始英文或错误代码，现在按界面语言显示。
- 批量创建全部成功时，不再提示「返回修改失败行」。
- 区域名超长时提示的是英文原始报错。

### 升级提示

1. 本次数据库变更为迁移 `0018` 至 `0024`，从 v1.0.0 升上来需要全部执行。
2. 迁移 `0021` 会删除 `users` 表的归属管理员列，没有回退脚本，升级前务必备份并验证备份可读。
3. 完整步骤、验证 SQL 与回退方案见 [docs/上线迁移-区域与自助注册.md](docs/上线迁移-区域与自助注册.md)。
4. 自助注册与 reCAPTCHA 升级后默认关闭，需要用的话在全站设置里填好两把密钥再打开。

## v1.0.0 — 2026-08-30 — 页面能力、联系方式与数据分析升级

这是一次覆盖公开个人页、后台编辑、账号管理、自动化接口和数据分析的整体升级。数据库变更包含迁移 `0013` 至 `0017`。

### 公开个人页与视觉体验

- 字体改为服务器自托管 WOFF2，减少外部字体依赖并改善加载稳定性。
- 调整头像、简介和按钮之间的留白；修正 Hero 等模板的简介对齐和文字可读性。
- 下线容易遮挡文字的 Cutout 布局，保留 Classic、Hero、Banner、Shape 四种布局。
- Banner 图片与头像拆分为独立媒体槽，可分别上传、裁切和更换。
- 新增 Ocean、Rose、Lavender、Sunset、Mono、Glass 等常用主题，以及 Glass Ocean、Glass Rose、Glass Aurora 玻璃配色。
- 重做玻璃效果的高光、模糊、边缘和层次；支持头像、Banner、背景图按用途裁切并重新调整。

### 联系方式与跳转

- 新增短信和拨打电话按钮；WhatsApp 与短信支持可选预设消息。
- WhatsApp、Messenger、Instagram、手机号等输入加入格式验证和标准化，WhatsApp 最终使用纯数字国际号码。
- Instagram 默认生成 `ig.me/m/{username}` 私信地址，也可关闭私信模式并链接到主页。
- Messenger 支持用户名和数字 Facebook ID；公开页增加 iOS、Android、桌面浏览器的智能唤起与网页回退。
- Instagram 与 WhatsApp 补齐分平台智能唤起：Instagram 私信保留 `ig.me/m/{username}` 的目标会话语义，并在 Android 显式指定 Instagram 应用以避免浏览器选择框；主页入口和 Android WhatsApp 再按平台增强，所有入口均保留 HTTPS 回退且不主动前往应用商店。
- 新增 Instagram、短信、电话品牌图标及相关点击统计。

### 页面管理与复制

- 页面列表改为紧凑的实时缩略预览卡片；点击预览直接进入编辑，预览不会产生页面浏览统计。
- 新增“复制页面”：复制布局、主题、简介、媒体和联系方式，访问及点击数据从零开始。
- 新增推广链接工具，可快速生成并复制带 `src` 来源参数的 TikTok、Instagram、YouTube、Facebook 等投放地址。
- 页面地址、登录用户名、访客看到的显示名字和后台备注采用明确分离的字段与编辑入口。
- 登录用户名支持修改、大小写不敏感唯一校验、会话失效和改名审计；管理员可维护用户显示名字与后台备注。
- 后台顶部导航改为固定标头，长页面滚动时仍可访问主要功能。

### 联系方式更新 API

- 每个个人页可以创建、查看和停用独立 API Key，支持有效期与权限范围。
- 新增 `GET/PATCH /_api/v1/profiles/{profileId}/contacts`，外部 CRM 或自动化程序可读取和局部更新联系方式。
- API 仅修改提交的平台，不覆盖页面样式、排序或统计；密钥只显示一次，服务端仅保存哈希，并记录调用审计。
- 完整调用格式见 [docs/external-contact-api.md](docs/external-contact-api.md)。

### 数据分析重构

- 分析对象改为三级：全量总览 → 单个账号及其个人页表现 → 单个个人页详细分析；总览可直接进入任意个人页。
- 总览新增等长上一周期对比、进入 → 点击 → 联系漏斗、跨账号个人页排行和自动运营结论。
- 个人页排行增加主要来源、主要国家、主要联系方式、周期变化，以及增长最快和待优化机会排序。
- 新增来源 × 联系方式矩阵和星期 × 小时热力图；来源与国家支持联动筛选。
- 单页指标统一为进入页面、按钮点击、联系点击、按钮点击率和联系转化率，并保留时间趋势与联系活跃时段。
- 新增来源 × 国家 × 联系方式交叉分析，可回答某平台、某国家分别产生了多少进入和联系点击。
- 新增来源转化排行、国家热力地图、主要国家排行和联系方式点击排行；详细联动表继续用于下钻。
- 国家和城市通过本地 GeoIP MMDB 识别；地图资产随管理端本地发布，不依赖运行时第三方地图服务。
- 总览新增全球国家分析：世界地图支持按打开、全部点击或联系点击着色；提供全部国家汇总、单国每日趋势，以及 WhatsApp、Messenger 等联系方式的每日点击明细。
- 管理端总览汇总全部可见个人页，并始终标明个人页及所属账号；账号汇总继续作为可选筛选。

### 部署与兼容性

- Docker 镜像包含自托管字体，GeoIP 目录通过只读卷挂载；缺少 MMDB 时服务仍可运行，但国家和城市为空。
- 新增数据库迁移：主题与独立 Banner、Instagram 私信设置、外部 API Key、登录用户名改名审计。
- 生产服务默认只绑定宿主机 `127.0.0.1:3000`，由 Nginx 对外提供 HTTPS。
- 已完成工作区 TypeScript 检查、生产构建与 398 项自动化测试，并验证应用与数据库健康状态。

### 升级提示

1. 拉取代码后执行数据库迁移，再重建应用镜像。
2. 若需要国家和城市分析，将兼容 MaxMind 格式的城市 MMDB 放到 `.env` 中 `GEOLITE2_HOST_DIR` 指定的目录。
3. API Key 明文只在创建时返回一次，请立即保存；遗失后应停用旧密钥并重新创建。
4. 旧 Banner 页面迁移时会先沿用原头像作为 Banner，之后头像与 Banner 可独立更换。
