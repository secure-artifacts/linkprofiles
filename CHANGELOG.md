# 更新记录

## v1.0.0 — 2026-08-30 — 页面能力、联系方式与数据分析升级

这是一次覆盖公开个人页、后台编辑、账号管理、自动化接口和数据分析的整体升级。数据库变更包含迁移 `0013` 至 `0017`。

### 公开个人页与视觉体验

- 字体改为服务器自托管 WOFF2，减少外部字体依赖并改善加载稳定性。
- 调整头像、简介和按钮之间的留白；修正 Hero 等模板的简介对齐和文字可读性。
- 下线容易遮挡文字的 Cutout 布局，保留 Classic、Hero、Banner、Shape 四种布局。
- Banner 图片与头像拆分为独立媒体槽，可分别上传、裁切和更换。
- 新增 Ocean、Rose、Lavender、Sunset、Mono、Glass 等常用主题，以及 Glass Ocean、Glass Rose、Glass Aurora 玻璃配色。
- 重做玻璃效果的高光、模糊、边缘和层次；支持头像、Banner、背景图按用途裁切并重新调整。

### 联系方式与跳转

- 新增短信和拨打电话按钮；WhatsApp 与短信支持可选预设消息。
- WhatsApp、Messenger、Instagram、手机号等输入加入格式验证和标准化，WhatsApp 最终使用纯数字国际号码。
- Instagram 默认生成 `ig.me/m/{username}` 私信地址，也可关闭私信模式并链接到主页。
- Messenger 支持用户名和数字 Facebook ID；公开页增加 iOS、Android、桌面浏览器的智能唤起与网页回退。
- 新增 Instagram、短信、电话品牌图标及相关点击统计。

### 页面管理与复制

- 页面列表改为紧凑的实时缩略预览卡片；点击预览直接进入编辑，预览不会产生页面浏览统计。
- 新增“复制页面”：复制布局、主题、简介、媒体和联系方式，访问及点击数据从零开始。
- 新增推广链接工具，可快速生成并复制带 `src` 来源参数的 TikTok、Instagram、YouTube、Facebook 等投放地址。
- 页面地址、登录用户名、访客看到的显示名字和后台备注采用明确分离的字段与编辑入口。
- 登录用户名支持修改、大小写不敏感唯一校验、会话失效和改名审计；管理员可维护用户显示名字与后台备注。
- 后台顶部导航改为固定标头，长页面滚动时仍可访问主要功能。

### 联系方式更新 API

- 每个个人页可以创建、查看和停用独立 API Key，支持有效期与权限范围。
- 新增 `GET/PATCH /_api/v1/profiles/{profileId}/contacts`，外部 CRM 或自动化程序可读取和局部更新联系方式。
- API 仅修改提交的平台，不覆盖页面样式、排序或统计；密钥只显示一次，服务端仅保存哈希，并记录调用审计。
- 完整调用格式见 [docs/external-contact-api.md](docs/external-contact-api.md)。

### 数据分析重构

- 分析对象改为三级：全部账号表现 → 单个账号及其个人页表现 → 单个个人页详细分析。
- 单页指标统一为进入页面、按钮点击、联系点击、按钮点击率和联系转化率，并保留时间趋势与联系活跃时段。
- 新增来源 × 国家 × 联系方式交叉分析，可回答某平台、某国家分别产生了多少进入和联系点击。
- 新增来源转化排行、国家热力地图、主要国家排行和联系方式点击排行；详细联动表继续用于下钻。
- 国家和城市通过本地 GeoIP MMDB 识别；地图资产随管理端本地发布，不依赖运行时第三方地图服务。
- 管理端汇总只比较账号，账号页只比较名下个人页，避免把不同投放对象的数据混在一起。

### 部署与兼容性

- Docker 镜像包含自托管字体，GeoIP 目录通过只读卷挂载；缺少 MMDB 时服务仍可运行，但国家和城市为空。
- 新增数据库迁移：主题与独立 Banner、Instagram 私信设置、外部 API Key、登录用户名改名审计。
- 生产服务默认只绑定宿主机 `127.0.0.1:3000`，由 Nginx 对外提供 HTTPS。
- 已完成工作区 TypeScript 检查、生产构建与 398 项自动化测试，并验证应用与数据库健康状态。

### 升级提示

1. 拉取代码后执行数据库迁移，再重建应用镜像。
2. 若需要国家和城市分析，将兼容 MaxMind 格式的城市 MMDB 放到 `.env` 中 `GEOLITE2_HOST_DIR` 指定的目录。
3. API Key 明文只在创建时返回一次，请立即保存；遗失后应停用旧密钥并重新创建。
4. 旧 Banner 页面迁移时会先沿用原头像作为 Banner，之后头像与 Banner 可独立更换。
